int jksd = 3;
int result = 0;

fn int l(double y, int h, string str, string hhh, string erty){
  jksd = jksd-1;
  result = ( 234 * h + jksd)/2;
  
  return result;
}
fn double Double(double r) {
	double result = 2.0*3.14*r*r;

	return result;
}
fn bool Bool(string r) {
	bool result = true;
	return result;
}
fn string String(double ret) {
	let result = "";
	return result;
}
fn int main()
{
	double resultFunction = Double(2.5);
	int kl = 10;
	int div = kl/0;
	let aaa = l(-0.8, 77, "hyeufhuhd", "kk", "I love Kate!!");
	
	if(0.34 > -3.4){
		let r = 9;
		if(r > jksd){
			r = 87;
		}
		else{
		r = 37;
		if(r > jksd){
			r = 87;
		}
		}
	}
	else{
	let r = 9;
	}
  @ let g = -0hAF;
   g = g * l(2.0+2.0, 7 + 4 * l(0.7, 7 % 1));@
   return 0;
}
@Выражения с целыми и дробными числами и вызовами функций@
@If, else с целыми числами, дробными числами и bool@
@Передачу параметров в функцию в виде выражения(содержащего вызов функции). Пока целые и дробные. Можно и bool@
@let@
@восьмиричные...@
@арифметика@
@деление нало не обработано@
@нельзя не передавать парметры - так надо@

@double result = 2*3.14*r*r; Ожидался тип double - так надо.В польской записи промежуточный результат с литералами@
@не высчитывается так как есть индентификатор@
@нельзя объявлять пустую строку(ругается в ассемблере)@